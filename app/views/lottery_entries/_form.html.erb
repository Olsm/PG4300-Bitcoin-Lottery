<%= form_for(@lottery_entry) do |f| %>
  <% if @lottery_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lottery_entry.errors.count, "error") %> prohibited this lottery_entry from being saved:</h2>

      <ul>
      <% @lottery_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_area :email %>
  </div>
  <div class="field">
    <%= f.label :bitcoin_address %><br>
    <%= f.text_area :bitcoin_address %>
  </div>
  <div class="field">
    <%= f.label :amount_charged %><br>
    <%= f.text_field :amount_charged %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
